# ColmiRingVRCBridge

Windows bridge for COLMI smart-ring telemetry and VRChat OSC output.

Primary development target: COLMI R06.

> Initial development is in progress. Stable code is promoted through `feature/*` -> `dev` -> `main`.
